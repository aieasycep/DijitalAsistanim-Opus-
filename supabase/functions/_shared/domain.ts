/**
 * Single import surface for shared domain code inside edge functions.
 *
 * Every function imports from here rather than reaching into `@da/domain`
 * directly, so the boundary between the Deno runtime and the workspace
 * packages is one file. If the deployment story ever changes — vendoring,
 * bundling, a published package — only this module moves.
 */
export {
  AppError,
  APPROVAL_ACTION_TYPES,
  APPROVAL_STATUSES,
  APPROVAL_TTL_MS,
  approvalExpiryFrom,
  assertTransition,
  buildIdempotencyKey,
  BRIEFING_KINDS,
  BRIEFING_SECTIONS,
  canTransition,
  checkLimit,
  comparePriority,
  DAY_MS,
  DEFAULT_RETENTION,
  DEFAULT_TIME_ZONE,
  detectConflicts,
  diffPayload,
  EMAIL_CATEGORIES,
  ERROR_CODES,
  estimateTimeSaved,
  evaluateFollowUp,
  evaluatePriority,
  evaluateReferralRedemption,
  extendBonus,
  extractDates,
  findFreeBlocks,
  fingerprintInput,
  followUpDueAt,
  GOOGLE_AUTH_PARAMS,
  GOOGLE_SCOPES,
  hasExternalSideEffect,
  hasFeature,
  HOUR_MS,
  IMPORTANCE_LEVELS,
  IMPORTANCE_RANK,
  initialScopes,
  isAppError,
  isExpired,
  isTerminal,
  isValidReferralCode,
  isWeekend,
  LIFE_EVENT_TYPES,
  looksLikeReplyExpected,
  MAX_APPROVAL_ATTEMPTS,
  MICROSOFT_AUTH_PARAMS,
  MICROSOFT_SCOPES,
  MINUTE_MS,
  missingScopesFor,
  nextLocalTimeOccurrence,
  normalizeReferralCode,
  OAUTH_ENDPOINTS,
  PLAN_LIMITS,
  PRO_ENTITLEMENT_ID,
  referralCodeFromBytes,
  resolveEntitlements,
  resolveReminderTime,
  RETENTION_DAYS,
  RETENTION_SWEEP,
  retentionCutoff,
  retryDelayMs,
  scopesFor,
  shouldRetry,
  startOfLocalDay,
  startOfLocalWeek,
  SUBSCRIPTION_STATUSES,
  suggestFocusBlock,
  summarizeDayLoad,
  systemClock,
  TIME_SAVED_COEFFICIENTS,
  toAppError,
  toIsoDate,
  toZonedParts,
  triage,
  validateEdit,
  verifyDateAgainstSource,
  zonedTimeToUtc,
  addLocalDays,
  endOfLocalDay,
} from '@da/domain'

export type {
  AccountKind,
  AnalyticsEvent,
  ApprovalAction,
  ApprovalActionType,
  ApprovalPayload,
  ApprovalStatus,
  Briefing,
  BriefingKind,
  BriefingSection,
  BriefingStats,
  CalendarEvent,
  Capture,
  CaptureIntent,
  Clock,
  Commitment,
  CommitmentDirection,
  ConnectedAccount,
  ConnectionStatus,
  Contact,
  EmailCategory,
  EmailMessage,
  EmailThread,
  Entitlements,
  ErrorCode,
  Feature,
  FollowUp,
  Importance,
  Insight,
  InsightAction,
  IsoDate,
  IsoInstant,
  LifeEvent,
  LifeEventType,
  Locale,
  MemoryChunk,
  NotificationCategory,
  Plan,
  PriorityCandidate,
  PriorityContext,
  PriorityResult,
  PriorityRule,
  PriorityRuleKind,
  Profile,
  Provider,
  ReminderPreset,
  ReplyTone,
  RetentionWindow,
  ScopeGroup,
  SourceRef,
  SourceType,
  SubscriptionStatus,
  SyncState,
  Task,
  TimedEvent,
  UserPreferences,
} from '@da/domain'
