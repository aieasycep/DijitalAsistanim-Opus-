import { FlagListSkeleton } from '@/components/flags'

/** The flag list's loading state, shaped like the page it precedes. */
export default function FlagsLoading() {
  return <FlagListSkeleton />
}
