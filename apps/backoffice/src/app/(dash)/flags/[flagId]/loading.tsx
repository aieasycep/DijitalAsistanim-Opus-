import { FlagDetailSkeleton } from '@/components/flags'

/** One flag's loading state, shaped like the page it precedes. */
export default function FlagDetailLoading() {
  return <FlagDetailSkeleton />
}
