import { GrantDetailSkeleton } from '@/components/grants'

/** One grant's loading state, shaped like the page it precedes. */
export default function GrantDetailLoading() {
  return <GrantDetailSkeleton />
}
