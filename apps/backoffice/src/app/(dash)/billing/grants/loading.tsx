import { GrantListSkeleton } from '@/components/grants'

/** The grant list's loading state, shaped like the page it precedes. */
export default function GrantsLoading() {
  return <GrantListSkeleton />
}
