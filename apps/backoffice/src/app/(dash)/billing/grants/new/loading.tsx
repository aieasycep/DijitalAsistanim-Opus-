import { GrantFormSkeleton } from '@/components/grants'

/** The grant form's loading state, shaped like the page it precedes. */
export default function NewGrantLoading() {
  return <GrantFormSkeleton />
}
