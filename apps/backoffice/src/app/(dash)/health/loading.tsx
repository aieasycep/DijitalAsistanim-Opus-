import { HealthDashboardSkeleton } from '@/components/health'

/** The health dashboard's loading state, shaped like the page it precedes. */
export default function HealthLoading() {
  return <HealthDashboardSkeleton />
}
