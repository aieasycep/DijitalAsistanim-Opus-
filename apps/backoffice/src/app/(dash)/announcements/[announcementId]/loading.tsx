import { AnnouncementDetailSkeleton } from '@/components/announcements'

/** The record's route-level loading state, in the loaded page's own rhythm. */
export default function AnnouncementDetailLoading() {
  return <AnnouncementDetailSkeleton />
}
