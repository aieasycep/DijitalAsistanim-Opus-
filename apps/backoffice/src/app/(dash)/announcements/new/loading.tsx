import { AnnouncementFormSkeleton } from '@/components/announcements'

/** The composer's route-level loading state: the form and its preview column. */
export default function NewAnnouncementLoading() {
  return <AnnouncementFormSkeleton />
}
