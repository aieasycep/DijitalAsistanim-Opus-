import { AdminListSkeleton } from '@/components/admins'

/** The roster's loading state, shaped like the page it precedes. */
export default function AdminsLoading() {
  return <AdminListSkeleton />
}
