import { InviteFormSkeleton } from '@/components/admins'

/** The invite form's loading state, shaped like the page it precedes. */
export default function InviteAdminLoading() {
  return <InviteFormSkeleton />
}
