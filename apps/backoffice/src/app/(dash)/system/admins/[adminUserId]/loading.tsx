import { AdminDetailSkeleton } from '@/components/admins'

/** One admin's loading state, shaped like the page it precedes. */
export default function AdminDetailLoading() {
  return <AdminDetailSkeleton />
}
