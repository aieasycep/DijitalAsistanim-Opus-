import { RoleMatrixSkeleton } from '@/components/admins'

/** The role matrix's loading state, shaped like the page it precedes. */
export default function RolesLoading() {
  return <RoleMatrixSkeleton />
}
