import { PromptDetailSkeleton } from '@/components/prompts'

/** One version's loading state, shaped like the page it precedes. */
export default function PromptDetailLoading() {
  return <PromptDetailSkeleton />
}
