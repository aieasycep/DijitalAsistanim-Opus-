import { PromptListSkeleton } from '@/components/prompts'

/** The version list's loading state, shaped like the page it precedes. */
export default function PromptsLoading() {
  return <PromptListSkeleton />
}
