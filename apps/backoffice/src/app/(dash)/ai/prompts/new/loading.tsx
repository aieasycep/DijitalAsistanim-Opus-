import { PromptFormSkeleton } from '@/components/prompts'

/** The composer's loading state: two short fields, the body, the note. */
export default function NewPromptLoading() {
  return <PromptFormSkeleton />
}
