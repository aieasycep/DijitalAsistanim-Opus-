node_modules
dist
build
.next
.expo
coverage
pnpm-lock.yaml
*.md.snap
apps/mobile/ios
apps/mobile/android
supabase/.temp

# Generated by `next build`, and it says so in its own header. Prettier had
# reformatted it once, so every build rewrote it back to Next's style and left
# the tree dirty — which fails `format:check` in CI for anyone who has built
# the console. Next owns the file; the repository stores exactly what Next
# emits.
apps/backoffice/next-env.d.ts
